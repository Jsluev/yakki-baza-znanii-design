# ЯККИ — правила сборки дизайна (конспект DESIGN2.md)

Источник истины: `DESIGN2.md` (вторая итерация), брендбук `YAKKI_BB_1.1.pdf`,
скрины Figma, папка `Логотип/`. Этот файл — рабочий конспект для сборки:
если правило здесь противоречит полному гайду — прав гайд.

## 0. Железные ограничения

- Функционал прототипа не меняется: роуты, обработчики, данные, фильтры.
  Меняются только CSS и декоративная разметка (бейджи, обёртки, иконки).
- Отчётный прототип заморожен: `C:\opencodework\Якки` + репо `yakki-baza-znanii`
  (https://jsluev.github.io/yakki-baza-znanii/). Не трогать.
- Дизайн — только здесь: `C:\opencodework\Якки-дизайн` + репо
  `yakki-baza-znanii-design` (https://jsluev.github.io/yakki-baza-znanii-design/).

## 1. Токены

```css
--color-page:#f5f3f8; --color-surface:#fff; --color-ink:#0b132b;
--color-text-muted:#706e73; --color-section-label:#767676;
--color-border:#d9d9d9; --color-placeholder:#aeaeae;
--color-purple:#a038b0; --color-purple-dark:#4f2b85; --color-orange:#f78e45;
--color-blue:#63adf2; --color-mint:#1b998b; --color-mint-dark:#20a57a;
--color-mint-light:#c5dfdc; --color-footer:#1e293b;
--gradient-primary:linear-gradient(90deg,#4f2b85,#a038b0);
--gradient-active:linear-gradient(90deg,#20a57a,#1aa679);
--radius-control:6px; --radius-content:12px; --radius-badge-small:3px;
--radius-pill:999px;
```

- Ритм: 3 / 6 / 12 / 18 / 24 / 36 / 42 / 46 / 48 / 81 / 117.
- Контейнеры: `.wrap` 1180 (по умолчанию), `.wrap-narrow` 1206 (архивы, кейс),
  `.wrap-wide` 1278 (ценности, результаты). На вьюпорте 1440 дают поля 117/81.
- Семантика цвета: пурпурный градиент — бренд и главные действия; оранжевый —
  инструменты; голубой — карточки ценностей; зелёный — выбор и связанные данные.
- Пурпур `#A038B0` на белом и оранжевый — только крупные акценты/бейджи,
  не мелкий текст (контраст).

## 2. Типографика

- Georgia 400 — ТОЛЬКО H1 страниц (54px/1.1) и название кейса в hero.
- Всё остальное — стек `'Museo Sans Cyrl','Nunito Sans',...` (файлов Museo нет,
  рендерится Nunito Sans; подключить woff2 одной строкой, когда появятся).
- Шкала: H1 54 / заголовок блока 30/700 / карточка 24/700/1.1 / навигация 18/500 /
  лид 18/300 / текст 16/300/1.3 / контролы 16/500 / служебный 16/700 uppercase /
  мета 14/500 / малый бейдж 12/700 uppercase.
- Без отрицательного letter-spacing и уплотнений ради попадания в блок.
- Карточки: фиксированная высота, длинные тексты — clamp, полный текст — внутри.

## 3. Компоненты

- **Шапка** 88px, белая: логотип-горизонталка 186×64; 3 пункта, центрированы,
  gap 36; active — градиентный текст + полоса 6px; ссылка 198×46 pill;
  chevron 18px; dropdown с aria-expanded/controls, клик + Esc (ховер тоже живёт).
- **Архивы**: сетка 324 + 84 + 2×390; фильтры белые r6/pad24, контролы 276×42
  на `#F5F3F8`, свой chevron; поиск — первый в сайдбаре; сброс — pill 42px;
  кнопка фильтров со счётчиком; карточки 390×350, клампы, ценность у низа.
- **Синглы (все)**: единый `detailHero()` — бейдж + H1 + лид + CTA + chips,
  градиент тонирован цветом раздела (кейс пурпур, инструмент оранжевый,
  методика фиолет, шаблон teal, документ violet, ценность pink,
  результат blue, глоссарий brown); контейнер 1206, сетка 798+390;
  h2-блоков 30px, subhead/sidebar-h4 16px, kv с hairline-разделителями —
  глобально для всех деталей. CTA: .md (есть ветки case/tool/methodology/
  template), JSON-экспорт для результата, иначе без CTA.
  Шапка БЕЗ поиска (как в макетах; поиск живёт на главной, в сайдбарах,
  мобильном меню; JS/вернуть — блок `.header-search`).
- **Карточки**: белые БЕЗ рамок; тегов в выдаче нет; счётчик
  «Найдено проектов: N».
- **Ценности**: 4×306 gap 18; карточка 306×280 r6/pad24; счётчик и кнопка 54px
  голубые, hover/focus — зелёные + зелёная тень; вся карточка — одна ссылка.
- **Результаты**: шапка H1+лид(528) слева, справа подпись+стрелка+CTA;
  браузер 349/349/580, шапка 43px mint, строки 43/45px, выбор — зелёный
  градиент, значения — зелёные 14px.
- **Футер** `#1E293B`, min-height 315, контейнер 1278, 4 зоны, соцсети 40px
  gap 12, соглашение — во 2-й колонке.
- **Крошки** 16px/500: корень — «ЯККИ», остальные уровни — opacity .5.
- **Мазки** — только фон: `position:absolute` в `main` (скроллятся со
  страницей, `z-index:-1`), `pointer-events:none`, пустой `alt`, vivid без
  opacity. Видны ТОЛЬКО на designed-страницах (base `display:none`):
  all — синий TR + оранжевый BL; cases — зелёный BL; tools — жёлтый TR;
  values — голубой BR. На остальных (включая результаты, синглы, главную) —
  скрыты. Используются ОРИГИНАЛЫ из `figma2209/*.png` (ужаты в `assets/`,
  белый вырезан в альфу); повороты — из CSS-координат макетов
  (all: −14°/−172°, cases: −18°).

## 4. Адаптив (принципы)

- Desktop-композиция перестраивается, не уменьшается; текст ≥16, служебный ≥14;
  тап-зоны ≥44px; горизонтального скролла нет.
- Текущие брейкпоинты 980/860/640 — покрывают требуемые ширины; менять только
  если контент упрётся.
- Mobile h1 оставлен 22px против переполнений (в гайде clamp 36–54 для кейса).

## 5. Принятые отступления от гайда

1. Навигация — 3 пункта («С чего начать», «Справочник» только в футере/главной):
   5 пунктов кеглем 18px физически не влезают между логотипом и кнопкой.
2. В карточках нет тег-рядов (в гайде их нет).
3. Mobile банка результатов — простое складывание, не drill-down.
4. Кнопки «Скачать» файлов — демо-заглушки, loading/error не делались.
5. Деталка результата оставлена на контейнере 1180 (вне scope гайда).

## 6. Открытые вопросы

- Нужны: URL VK/Telegram сообществ; лицензионные woff2 Museo Sans Cyrl;
  редакторская проверка подписи у JSON-кнопки.
- Скриншотного контроля нет — визуал проверять глазами на живых страницах.
